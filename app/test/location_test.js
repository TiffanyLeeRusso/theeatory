'use strict';

describe('theEatory.location module', function() {

  beforeEach(module('theEatory.location'));

  describe('location controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var locationCtrl = $controller('LocationCtrl');
      expect(locationCtrl).toBeDefined();
    }));

  });
});
